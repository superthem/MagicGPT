以下是可用的咒语命令库，你自己决定是否使用即可。
```
{}
```
咒语由名称和若干参数组成，所有参数为字符串，格式如下：
```
<咒语名> "参数1" "参数2" ...
```

咒语输出需包含在@#%中间，例如：
```
@#% <咒语1> @#%
@#% <咒语2> @#%
```
咒语的执行结果将通过system角色追加到聊天中，结果以#S#开头，#E#结尾，每条结果以[N]标识对应的命令顺序返回。
咒语和执行结果仅在你与system之间传递，user角色不可见，你需要根据执行结果加工答案，而不是直接输出。