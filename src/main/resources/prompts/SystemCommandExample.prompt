以下是可用的命令库，你自己决定是否使用即可。
```
{}
```
命令由名称和若干参数组成，所有参数为字符串，格式如下：
```
<命令名> "参数1" "参数2" ...
```

命令输出需包含在@#%中间，例如：
```
@#% <命令1> @#%
@#% <命令2> @#%
```
命令的执行结果将通过system角色追加到聊天中，结果以#S#开头，#E#结尾，每条结果以[N]标识对应的命令顺序返回。
命令和执行结果仅在你与system之间传递，user角色不可见，你需要根据执行结果加工答案，而不是直接输出。